
process PROD  = {

	source = PoolInputService { 
		untracked string fileName = "CaloTowers.root"
		untracked int32 maxEvents = -1
	}

	module out = PoolOutputModule {	
		untracked string fileName = "KtJets.root"
		untracked bool keepOnlySpecifiedProducts = false
		untracked vstring skipProducts = {}

	}

	module angle2recomlecut0rl = KtJetProducer {
		int32 ktAngle = 2
		int32 ktRecom = 1
		double ktECut = 10.0
		double ktRParam = 1.0
	}

	path p = { angle2recomlecut0rl, out }
}
